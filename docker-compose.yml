version: "3.9"
services:
  gotapp:
    build: ./first-app
    volumes:
      - ./build:/usr/build
  rnmapp:
    build: ./second-app
    volumes:
      - ./build:/usr/build
  navbar:
    build: ./navbar
    volumes:
      - ./build:/usr/build
  root:
    build: ./root-config
    volumes:
      - ./build:/usr/build
